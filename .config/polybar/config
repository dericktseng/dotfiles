# colors are aarrggbb
[colors]
black = #565656
blue = #1EA5F7
grey = #AAAAAA
white = #FFFFFF
lightblue = #DEEDF0
darkblue = #2B5CEB
yellow = #ffb52a
green = #98DDCA
lightgreen = #D5ECC2
orange = #FFD3B4
purple = #C9CBFF
red = #FFAAA7
glass = #FFFFFF

[fonts]
font-0 = Inconsolata SemiBold:style=Regular:pixelsize=13;3
font-1 = FiraCode Nerd Font Mono:style=bold:pixelsize=12;4
font-2 = FiraCode Nerd Font Mono:style=Regular:pixelsize=16;4
font-3 = DejaVu Sans:style=Regular:pixelsize=13;4

[bar/main]
wm-restack = bspwm
enable-ipc = true
override-redirect = false
width = 100%
height = 30
radius = 0.0
offset-x = 0
offset-y = 0
fixed-center = true
bottom = true
background = ${colors.glass}
foreground = ${colors.black}
border-color = ${colors.black}
border-top-size = 1

font-0 = ${fonts.font-0}
font-1 = ${fonts.font-1}
font-2 = ${fonts.font-2}
font-3 = ${fonts.font-3}

line-size = 3
module-margin = 0

modules-left = powermenu bspwm
modules-center = date
modules-right = pulseaudio battery backlight cputemperature gputemperature

tray-position = right
tray-padding = 0
tray-background = ${colors.lightblue}

cursor-click = pointer
cursor-scroll = ns-resize

[module/bspwm]
type = internal/bspwm

; Only show workspaces defined on the same output as the bar
; NOTE: The bspwm and XRandR monitor names must match, which they do by default.
; But if you rename your bspwm monitors with bspc -n this option will no longer
; behave correctly.
; Default: true
pin-workspaces = true

; Output mode flags after focused state label
; Default: false
inline-mode = false

; Create click handler used to focus workspace
; Default: true
enable-click = false

; Create scroll handlers used to cycle workspaces
; Default: true
enable-scroll = false

; Set the scroll cycle direction 
; Default: true
reverse-scroll = false

; Use fuzzy (partial) matching on labels when assigning 
; icons to workspaces
; Example: code;♚ will apply the icon to all workspaces 
; containing 'code' in the label
; Default: false
fuzzy-match = false

; ws-icon-[0-9]+ = <label>;<icon>
; Note that the <label> needs to correspond with the bspwm workspace name
; Neither <label> nor <icon> can contain a semicolon (;)

; Available tags:
;   <label-monitor>
;   <label-state> - gets replaced with <label-(focused|urgent|occupied|empty)>
;   <label-mode> - gets replaced with <label-(monocle|tiled|fullscreen|floating|locked|sticky|private)>
; Default: <label-state>
format = <label-state> <label-mode>

; Available tokens:
;   %name%
; Default: %name%
label-monitor = %name%

ws-icon-0 = 1;1
ws-icon-1 = 2;2
ws-icon-2 = 3;3
ws-icon-3 = 4;4
ws-icon-4 = 5;5
ws-icon-5 = 6;6
ws-icon-6 = 7;7
ws-icon-7 = 8;8
ws-icon-8 = 9;9
ws-icon-9 = 10;0

ws-icon-10 = browser;%{T3}%{T-}
ws-icon-11 = terminal;%{T3}%{T-}
ws-icon-12 = pdf;%{T3}%{T-}
ws-icon-13 = discord;%{T3}ﭮ%{T-}
ws-icon-14 = folder;%{T3}%{T-}
ws-icon-15 = video;%{T3}嗢%{T-}
ws-icon-16 = zoom;%{T3}%{T-}
ws-icon-17 = game;%{T3}%{T-}
ws-icon-18 = office;%{T3}%{T-}
ws-icon-19 = paint;%{T3}%{T-}
ws-icon-20 = minecraft;%{T3}%{T-}
ws-icon-21 = star;%{T3}%{T-}

label-mode-padding = 2
label-mode-foreground = ${colors.black}
label-mode-background = ${colors.yellow}

; focused = Active workspace on focused monitor
label-focused = %icon%
label-focused-foreground = ${colors.black}
label-focused-background = ${colors.lightblue}
label-focused-underline = ${colors.blue}
label-focused-padding = 2
label-focused-font = 2

; unfocused = Inactive workspace on any monitor
label-occupied = %icon%
label-occupied-padding = 2
label-occupied-font = 2

; empty = Active workspace on unfocused monitor
label-empty = %icon%
label-empty-padding = 2
label-empty-foreground = ${colors.grey}
label-empty-font = 2

; urgent = Workspace with urgency hint set
label-urgent = %icon%
label-urgent-foreground = ${colors.black}
label-urgent-background = ${colors.red}
label-urgent-padding = 2
label-urgent-font = 2

[module/date]
type = internal/date
interval = 1

date = %a %b %d
date-alt = %a %b %d

time = %I:%M %p
time-alt = %I:%M:%S
label = %{T3}%{T-} %time% | %date%
label-padding = 1

[module/pulseaudio]
type = internal/pulseaudio

format-volume = %{T3}<ramp-volume>%{T-} <label-volume>
format-volume-background = ${colors.purple}
format-volume-padding = 1

ramp-volume-0 = 
ramp-volume-1 = 
ramp-volume-2 = 

label-volume = %percentage%%

label-muted = %{T3}婢%{T-} muted
label-muted-background = ${colors.purple}
label-muted-foreground = ${colors.grey}
label-muted-padding = 1


[module/backlight]
type = internal/backlight
format-foreground = ${colors.black}
format-background = ${colors.lightgreen}

; Use the following command to list available cards:
; $ ls -1 /sys/class/backlight/
card = amdgpu_bl0

; (unreleased)
; Use the `/sys/class/backlight/actual-brightness` file
; rather than the regular `brightness` file.
; Defaults to true unless the specified card is an amdgpu backlight.
use-actual-brightness = true

; Enable changing the backlight with the scroll wheel
; NOTE: This may require additional configuration on some systems. Polybar will
; write to `/sys/class/backlight/${self.card}/brightness` which requires polybar
; to have write access to that file.
; DO NOT RUN POLYBAR AS ROOT. 
; The recommended way is to add the user to the
; `video` group and give that group write-privileges for the `brightness` file.
; See the ArchWiki for more information:
; https://wiki.archlinux.org/index.php/Backlight#ACPI
; Default: false
enable-scroll = false
format = <ramp> <label>
format-padding = 1

; Available tokens:
;   %percentage% (default)
label = %percentage%%

; Only applies if <ramp> is used
ramp-0 = 🌕
ramp-1 = 🌔
ramp-2 = 🌓
ramp-3 = 🌒
ramp-4 = 🌑


[module/battery]
type = internal/battery
battery = BAT0
adapter = AC
full-at = 100

format-charging = %{A1:$HOME/.config/polybar/batteryinfo.sh --time:}<label-charging>%{A}
format-charging-foreground = ${colors.black}
format-charging-background = ${colors.green}
label-charging =  %percentage%%
format-charging-padding = 1

format-discharging = %{A1:$HOME/.config/polybar/batteryinfo.sh --time:}<ramp-capacity> <label-discharging>%{A}
format-discharging-foreground = ${colors.black}
format-discharging-background = ${colors.green}
label-discharging = %percentage%%
format-discharging-padding = 1

format-full = %{A1:$HOME/.config/polybar/batteryinfo.sh:}<label-full>%{A}
format-full-foreground = ${colors.black}
format-full-background = ${colors.green}
label-full =  100%
format-full-padding = 1

format-low = %{A1:$HOME/.config/polybar/batteryinfo.sh:}<label-low>%{A}
format-low-foreground = ${colors.black}
format-low-background = ${colors.green}
label-low =  %percentage%%
format-low-padding = 1

ramp-capacity-0 = 
ramp-capacity-1 = 
ramp-capacity-2 = 
ramp-capacity-3 = 
ramp-capacity-4 = 
ramp-capacity-5 = 
ramp-capacity-6 = 
ramp-capacity-7 = 
ramp-capacity-8 = 
ramp-capacity-9 = 
ramp-capacity-foreground = ${colors.black}

[module/cputemperature]
type = custom/script
format = %{T3}%{T-} <label>
label = %output%
format-padding = 1
exec = $HOME/.config/polybar/thermalcpu.sh
format-foreground = ${colors.black}
format-background = ${colors.orange}
interval = 3

[module/gputemperature]
type = custom/script
format = %{T3}﬙%{T-} <label>
label = %output%
format-padding = 1
exec = $HOME/.config/polybar/thermalgpu.sh
format-foreground = ${colors.black}
format-background = ${colors.red}
interval = 3

[module/powermenu]
type = custom/text
content = %{T3}襤%{T-}
content-foreground = ${colors.white}
content-background = ${colors.blue}
content-padding = 2
click-left = ~/.config/rofi/powermenu.sh

[settings]
screenchange-reload = true
;compositing-background = xor
;compositing-background = screen
;compositing-foreground = source
;compositing-border = over
;pseudo-transparency = false

[global/wm]
margin-top = 0
margin-bottom = 0

; vim:ft=dosini
